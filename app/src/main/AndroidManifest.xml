<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.common.project">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    11
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.ForgetPasswordActivity" />
        <activity android:name=".activity.ChooseBookActivity" />
        <activity android:name=".activity.SetStudyCountPlanActivity" />
        <activity android:name=".activity.StartRememberWordActivity" />
        <activity android:name=".activity.ReviewWordActivity" />
        <activity android:name=".activity.NewWordBookActivity" />
        <activity android:name=".activity.StudyRemindActivity" />
        <activity android:name=".activity.WordBookActivity" />
        <activity android:name=".activity.MyDataActivity" />
        <activity android:name=".activity.WordDetailActivity" />
        <activity android:name=".activity.StudyPlanActivity"></activity>
    </application>

</manifest>